<section class="panel output-category-panel"><div class="panel-heading"><p class="eyebrow">Materiales planificados</p><h2>Consumo aplicado al plan</h2><p>Solo se muestra la cantidad que atendió el requerimiento previsto de la orden.</p></div>
@if ($detallesPlanificados->isEmpty())<div class="output-category-empty"><x-ui.icon name="inventory" :size="24" /><div><strong>Sin consumo planificado aplicado</strong><span>Esta salida no atendió materiales previstos de una orden.</span></div></div>@else<div class="output-category-list">@foreach ($detallesPlanificados as $detalle)<article><div><a href="{{ route('productos.show', $detalle->producto_id) }}">{{ $detalle->producto?->codigo }}</a><span>{{ $detalle->producto?->descripcion }}</span></div><dl><div><dt>Aplicado al plan</dt><dd><x-ui.quantity :value="$detalle->cantidad_planificada_aplicada" /> {{ $detalle->producto?->unidadMedida?->codigo }}</dd></div><div><dt>Reserva aplicada</dt><dd><x-ui.quantity :value="$detalle->cantidad_aplicada_reserva" /></dd></div><div><dt>Repisa</dt><dd>{{ $detalle->repisa?->codigo ?? '—' }}</dd></div></dl></article>@endforeach</div>@endif</section>
