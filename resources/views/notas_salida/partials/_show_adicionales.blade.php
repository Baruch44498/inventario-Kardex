<section class="panel output-category-panel"><div class="panel-heading"><p class="eyebrow">Materiales adicionales</p><h2>Consumo no planificado o excedente</h2><p>Los excesos conservan el motivo operativo que autorizó la diferencia.</p></div>
@if ($detallesAdicionales->isEmpty())<div class="output-category-empty"><x-ui.icon name="check-circle" :size="24" /><div><strong>Sin materiales adicionales</strong><span>Todo el consumo de la salida quedó dentro del plan.</span></div></div>@else<div class="output-category-list">@foreach ($detallesAdicionales as $detalle)@php($cantidadAdicional = (float) $detalle->cantidad_excedente > 0 ? $detalle->cantidad_excedente : $detalle->cantidad)<article><div><a href="{{ route('productos.show', $detalle->producto_id) }}">{{ $detalle->producto?->codigo }}</a><span>{{ $detalle->producto?->descripcion }}</span></div><dl><div><dt>Cantidad adicional</dt><dd><x-ui.quantity :value="$cantidadAdicional" /> {{ $detalle->producto?->unidadMedida?->codigo }}</dd></div><div><dt>Motivo</dt><dd>{{ $detalle->motivoExcedenteVisible() ?? 'Consumo real no planificado' }}</dd></div><div><dt>Observación</dt><dd>{{ $detalle->observacion ?: '—' }}</dd></div></dl></article>@endforeach</div>@endif</section>
