<section class="panel output-category-panel"><div class="panel-heading"><p class="eyebrow">Devoluciones utilizables</p><h2>Material que regresó al stock</h2><p>Estos retornos confirmados reducen el consumo real porque volvieron a estar disponibles.</p></div>
@if ($devolucionesUtilizables->isEmpty())<div class="output-category-empty"><x-ui.icon name="entry" :size="24" /><div><strong>Sin devoluciones utilizables</strong><span>No hay retornos confirmados que hayan incrementado nuevamente el stock.</span></div></div>@else<div class="output-category-list">@foreach ($devolucionesUtilizables as $retorno)<article><div><a href="{{ route('notas-ingreso.show', $retorno->nota_ingreso_id) }}">{{ $retorno->notaIngreso?->codigo }}</a><span>{{ $retorno->producto?->descripcion }}</span></div><dl><div><dt>Cantidad devuelta</dt><dd><x-ui.quantity :value="$retorno->cantidad" /> {{ $retorno->producto?->unidadMedida?->codigo }}</dd></div><div><dt>Efecto</dt><dd><span class="badge badge--success">Incrementó stock</span></dd></div><div><dt>Fecha</dt><dd>{{ $retorno->notaIngreso?->fecha_ingreso?->format('d/m/Y') ?? '—' }}</dd></div></dl></article>@endforeach</div>@endif</section>
